export const XFOLD_CYCLE_MS: number;
export const XFOLD_REST_FRAME: number;
/** Trusted, internally generated SVG markup; no user-provided content. */
export function renderXFoldFrame(timeMs: number): string;
